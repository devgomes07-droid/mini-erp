services:
  postgres:
    image: postgres:16
    container_name: mini-erp-db
    environment:
      POSTGRES_DB: minierp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - mini-erp-data:/var/lib/postgresql/data

volumes:
  mini-erp-data: